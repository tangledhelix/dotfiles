- name: "Install misc files"
  copy:
    src: "{{ dotfiles }}/{{ item }}"
    dest: "{{ ansible_user_dir }}/.{{ item }}"
    mode: 0644
  with_items:
    - colordiffrc
    - emacs
    - perldb
    - perltidyrc
    - pgclirc
    - psqlrc
    - screenrc
    - tcshrc
    - tmux.conf
  tags:
    - misc
